
import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

// Note: The React entry point is not being used in this vanilla HTML/CSS/JS project

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>This is a standalone HTML project - see index.html</div>
  </React.StrictMode>,
)
